export type MainInfo = {
  isDay: boolean;
  currentNetwork: string;
  uptime: number;
  notifications: number;
  temp: number;
  humidity: number;
  light_val: number;
  pump_good: boolean;
  light_good: boolean;
  liquid_good: boolean;
};
